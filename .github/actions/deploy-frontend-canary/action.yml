name: Deploy Frontend Canary
description: Build and deploy the frontend worker to Cloudflare Workers (canary environment)

inputs:
  display-name:
    description: Human-readable name shown in GitHub UI
    required: true

runs:
  using: deno
  main: run.ts
