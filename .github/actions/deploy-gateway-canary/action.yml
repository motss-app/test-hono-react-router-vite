name: Deploy Gateway Canary
description: Build, deploy, and verify the public canary gateway.

runs:
  using: composite
  steps:
    - name: Run Gateway Canary deployment
      shell: bash
      env:
        ACTION_PATH: ${{ github.action_path }}
      run: |
        set -euo pipefail
        deno run -A "${ACTION_PATH}/run.ts"
