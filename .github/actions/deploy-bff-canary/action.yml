name: Deploy BFF Canary
description: Build and deploy the BFF worker to Cloudflare Workers (canary).

runs:
  using: composite
  steps:
    - name: Run BFF Canary deployment
      shell: bash
      env:
        ACTION_PATH: ${{ github.action_path }}
      run: |
        set -euo pipefail
        deno run -A "${ACTION_PATH}/run.ts"
