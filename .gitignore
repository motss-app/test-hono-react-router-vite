.DS_Store
.env
.env.local
.env.sentry-build-plugin
.dev.vars
.dev.vars.*
.wrangler/
dist/
node_modules/

# React Router
/.react-router/
/packages/frontend/.react-router/
/packages/frontend/app/+types/
/packages/frontend/app/**/+types/

# AI coding assistant internal data
__screenshots__
.codex/
.tmp/
.vitest-attachments

# Git worktrees (e.g. created by AI agents via `git worktree add`)
.worktrees/

# Vite temp files
*.timestamp-*.mjs
coverage/

# Build artifacts: the root Vite/React Router build, Rust worker builds, and
# the generated wasm-bindgen glue (regenerate with `deno task
# build:fractal-wasm`; CI builds it via .github/actions/build-fractal-wasm).
packages/*/target/
**/build/
packages/frontend/app/labs-wasm/
.playwright-mcp/
