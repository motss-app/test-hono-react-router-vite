import { style } from '@vanilla-extract/css';

export const overflowHidden = style({
  overflow: 'hidden',
});
export const overflowVisible = style({
  overflow: 'visible',
});
export const overflowAuto = style({
  overflow: 'auto',
});
export const overflowClip = style({
  overflow: 'clip',
});
export const overflowXAuto = style({
  overflowInline: 'auto',
});
export const overflowYAuto = style({
  overflowBlock: 'auto',
});
