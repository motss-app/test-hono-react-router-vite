import { style } from '@vanilla-extract/css';

export const bgCover = style({
  backgroundSize: 'cover',
});
export const bgContain = style({
  backgroundSize: 'contain',
});
export const bgAuto = style({
  backgroundSize: 'auto',
});
export const bgNoRepeat = style({
  backgroundRepeat: 'no-repeat',
});
export const bgRepeat = style({
  backgroundRepeat: 'repeat',
});
export const bgRepeatX = style({
  backgroundRepeat: 'repeat-x',
});
export const bgRepeatY = style({
  backgroundRepeat: 'repeat-y',
});
export const bgFixed = style({
  backgroundAttachment: 'fixed',
});
export const bgLocal = style({
  backgroundAttachment: 'local',
});
export const bgScroll = style({
  backgroundAttachment: 'scroll',
});
