[package]
name = "fractal-rust"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
worker = "0.8.5"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
js-sys = "0.3"
# Shared Mandelbrot kernel also compiled into the browser-facing WASM module.
fractal-wasm = { path = "../fractal-wasm" }
png = "0.18.1"

[profile.release]
# `strip = true` breaks wasm-bindgen ("externref table required for catch
# wrappers", workers-rs #1014) — do not enable it.
lto = true
codegen-units = 1
opt-level = "z"
panic = "abort"
